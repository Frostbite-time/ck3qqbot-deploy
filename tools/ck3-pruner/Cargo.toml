[package]
name = "ck3-pruner"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
regex = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.3", features = ["formatting"] }

[dev-dependencies]
tempfile = "3.23"
